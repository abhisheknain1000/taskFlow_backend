MONGO_URI=mongodb+srv://<user>:<password>@<cluster>/<dbname>
JWT_SECRET=use-a-long-random-secret-in-production
JWT_EXPIRES_IN=90d
PORT=5000

# Frontend URL for CORS (no trailing slash, no duplicate CLIENT_URL= prefix)
CLIENT_URL=https://taskflow-ten-delta.vercel.app
